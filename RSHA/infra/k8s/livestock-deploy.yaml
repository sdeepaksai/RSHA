apiVersion: apps/v1
kind: Deployment
metadata:
  name: livestock-service
  namespace: rsha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: livestock-service
  template:
    metadata:
      labels:
        app: livestock-service
    spec:
      containers:
        - name: livestock-service
          image: sdeepaksai/livestock-service:1.0.0
          ports:
            - containerPort: 3001
          env:
            - name: DB_HOST
              value: postgres
---
apiVersion: v1
kind: Service
metadata:
  name: livestock-service
  namespace: rsha
spec:
  selector:
    app: livestock-service
  ports:
    - port: 80
      targetPort: 3001
